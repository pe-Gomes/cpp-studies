// Includes imports packages for using on the program
#include <iostream>

int main()
{
  // Cout prits to the cmd line and endl finishesh the line on the terminal
  std::cout << "Hello, world!" << std::endl;

  for (int i = 1; i <= 10; i++)
  {
    std::cout << i << std::endl;
  }

  return 0;
}